# 03 ROUTES — 3D 월드 FX

**전제:** 아래 라우팅은 `09_PROBE.md`가 아직 안 돌았다는 상태에서 쓴 것이다. B·D·E는 미검증 전제 위에 있다. **프로브 결과가 분기를 확정한다.**

---

## 1. 여섯 경로

| 경로 | 정체 | 커스텀 아트 | 아트디렉션 통제권 | 런타임 생성 | 현재 사용 |
|---|---|---|---|---|---|
| **A** `VFXPreset` | 공식 기성 146개. `PresetName` 한 줄 | ✗ | **`Color`/`Size`/`Transparency` 셋뿐** | 무타입 (미검증) | 0건 |
| **B** `VFXRecipe` | Niagara 39소스 3레이어. 템플릿 7종. `SetParam` | 간접 (E 경유) | 소스 교체·색·강도·딜레이 | 무타입 (미검증) | 0건 |
| **C** `ParticleEmitter` + 플립북 | 우리가 구운 아틀라스. Grid 2x2/4x4/8x8 | ✓ | **NumberSequence 커브 전부** | **정식 지원** | 0건 |
| **D** `Beam` / `Trail` | 텍스처 리본. **엔진 유일의 UV 스크롤** | ✓ | 폭·색·스크롤 | 무타입 (미검증) | 0건 |
| **E** `EmptySprite` (B 하위) | **임의 행×열 플립북 + `Size2D` 비정사각** | ✓ | 최고 | B에 종속 | 0건 |
| **F** 절차적 `Part` | Neon/Plastic을 코드로 생성·페이드 | 해당 없음 | 최고, 단 **파츠 수가 곧 fps** | 정식 지원 | **출시분 100%** |

**"통제권" 칸이 프리셋의 진짜 비용이다.** 146개는 `Adventure`/`Cartoon`/`Fantasy`/`Sci-Fi`/`Casual`/`Horror` 장르 태그가 붙은 남의 게임 톤이다. 조선·산곡의 절제된 톤과 맞는지는 **눈으로 봐야** 안다.

---

## 2. 결정 트리

```
이 이펙트는 무엇인가?

├─ 선·궤적·리본인가? (칼 궤적, 화살 꼬리, 레이저, 로프, 낙뢰 줄기)
│   └─ ▶ D (Beam / Trail).  UV 스크롤이 필요하면 여기가 유일한 길이다.
│         Beam  = 고정된 두 점 사이 (Attachment0/1 필수)
│         Trail = 움직이는 물체 뒤   (Attachment 여부 미검증 — 프로브)
│
├─ 파티클로 표현 불가능한 기하인가? (용의 머리, 부서진 바위, 실제 화살, 지형 파편)
│   └─ ▶ F (절차적 Part).  RyunochiDragon.lua 가 선례.
│         예산은 06_TERRAIN.md §4 를 지켜라.
│
├─ 흩뿌려지는 입자·연기·불·안개·먼지인가?
│   │
│   ├─ 146개 프리셋(05_PRESET_MAP.md)에 이름이 맞는 게 있나?
│   │   │
│   │   ├─ 있다 + 톤도 맞다 + 색/크기만 조절하면 된다
│   │   │   └─ ▶ A (VFXPreset).  가장 싸다. 여기서 끝내라.
│   │   │
│   │   ├─ 있다 + 톤이 안 맞는다 (너무 만화적/화려함)
│   │   │   └─ ▶ B (VFXRecipe).  비슷한 템플릿을 복사해 Color/Alpha 키포인트만
│   │   │         우리 팔레트로 갈고, SpawnCount 를 낮춰 밀도를 죽여라.
│   │   │
│   │   └─ 없다 (또는 B로도 안 나온다)
│   │       │
│   │       ├─ 우리만의 실루엣이 필요한가? (조선 기와 먼지, 솔잎, 특정 무기 화염)
│   │       │   └─ ▶ C (ParticleEmitter + 우리 아틀라스)
│   │       │         비정사각/자유격자가 꼭 필요하면 ▶ E — 단 제약 있음(05 §5)
│   │       │
│   │       └─ 일반적인 모양이면 되는데 프리셋에 없다
│   │           └─ ▶ C, 단 CC0 플립북을 먼저 뒤져라. 굽지 말고 받아라.
│   │
│   └─ (프로브 전이라 A/B가 되는지 모르겠다)
│       └─ ▶ 09_PROBE.md 를 먼저 돌려라. 추측으로 F 를 쓰지 마라 —
│             그게 지금 이 프로젝트가 처한 상태다.
│
└─ 위 어디에도 안 맞는다
    └─ ▶ 10_OPEN_QUESTIONS.md 에 항목을 추가하고 사람에게 물어라.
          임의로 F 로 떨어뜨리지 마라. F 는 폴백이지 기본값이 아니다.
```

---

## 3. 아틀라스 예산 — C·E 경로의 1차 필터

업로드 권장 512 px 기준:

| 레이아웃 | 프레임당 | 읽히는 것 | 안 읽히는 것 |
|---|---|---|---|
| `Grid2x2` (4) | 256 px | 큰 임팩트 플래시, 링 | 미묘한 변화 |
| **`Grid4x4` (16)** | **128 px** | **연기, 먼지, 물튐, 화염 — 대부분** | 가는 선, 잔글씨 |
| `Grid8x8` (64) | 64 px | 안개, 뭉개져도 되는 연기 | 실루엣이 중요한 모든 것 |

**"이 기법이 128 px에서 읽히는가"가 C 경로 채택의 1차 관문이다.** 안 읽히면 프레임 수를 줄이고(4x4→2x2) 해상도를 벌어라. 체감 속도는 `FlipbookFramerate`와 `Lifetime`으로 맞출 수 있지만 **해상도는 못 만들어낸다.**

1024가 업로드되면 4x4에서 256 px가 되어 판이 달라진다 — **프로브 항목.**

---

## 4. 포스트FX가 0인 환경에서 "빛나 보이게" 하기

Bloom·ColorCorrection·Blur·DOF·SunRays **전부 없다.** 전역 손잡이는 `Lighting.Brightness`/`Contrast`/`Saturation`뿐. 발광은 전부 소재 레벨에서 만든다:

| 수단 | 어디에 | 비고 |
|---|---|---|
| `Material.Neon` | `BasePart` | 출시분이 전부 이걸 쓴다. **유일하게 검증된 발광** |
| `Material.Unlit` | `BasePart` | 조명 무시. Neon과 비교 필요 — 프로브 |
| `MaterialVariant.Emissive` + `EmissiveIntensity` + `EmissiveMap` | `BasePart.MaterialVariant` (문자열) | **로블록스에 없는 기능. 세기 조절이 되는 유일한 발광** |
| `ParticleEmitter.LightEmission` 0→1 | 파티클 | 0 = 알파블렌드, 1 = 가산합성. **회색조 시트를 가산으로 쓰면 알파 채널 없이도 빛난다** |
| `ParticleEmitter.Brightness` | 파티클 | 로블록스에 없는 추가 프로퍼티. 의미 미검증 — 프로브 |
| **글로우를 텍스처에 미리 구워 넣기** | 아틀라스 | 블룸이 없으니 이게 제일 확실하다 |
| `PointLight` | 실제 조명 | 비싸다. 히어로 순간에만 (정자 등불, 총구) |

**`ZOffset`이 없어서 불을 연기 앞에 강제로 둘 수 없다.** 겹침 순서에 의존하는 설계를 피하고, 필요하면 이미터를 카메라 쪽으로 물리적으로 오프셋해라.

---

## 5. 죽는 기법 — 시도하지 마라

전부 커스텀 셰이더가 필요하고, 이 엔진엔 셰이더도 머티리얼 그래프도 없다.

| 기법 | 왜 죽나 | 이 엔진에서 대신 |
|---|---|---|
| 모션벡터 플립북 | 프레임 보간 셰이더 필요 | 프레임 수를 늘리거나(`Grid8x8`) `FlipbookFramerate`를 낮춰 끊김을 감춰라 |
| 소프트/뎁스 페이드 파티클 | 씬 깊이 접근 필요 | 스프라이트 하단을 지면에 살짝 묻어 교차선을 숨겨라 (`08_ART_RULES` 규칙 4) |
| 버텍스 애니메이션 텍스처(VAT) | 버텍스 셰이더 필요 | 파츠 계층 + `CFrame` 키프레임 (`RyunochiDragon.lua` 방식) |
| 런타임 디졸브/침식 | 프래그먼트 셰이더 필요 | 디졸브를 **플립북으로 미리 구워라** |
| SDF 왜곡 / 열 아지랑이 | UV 오프셋 셰이더 필요 | 없다. 왜곡된 프레임을 구운 플립북으로 흉내 |
| 6-way lighting 연기 | 커스텀 라이팅 필요 | 조명 방향을 하나 정해 텍스처에 구워라 |
| 표면 UV 스크롤 | `Decal`/`Texture` 클래스 부재 → `OffsetStudsU/V` 없음 | **`Beam`/`Trail`의 `TextureSpeed`가 유일** (경로 D) |
| 포스트 프로세싱 전반 | API 0건 | §4 |

**이것들은 오프라인 시네마틱 렌더에서는 전부 유효하다. 인게임과 혼동하지 마라.**

---

## 6. 아틀라스를 굽는다면

블렌더에서 스프라이트시트를 굽는 경우의 함정:

- **Blender 4.2+에서 Alpha Blend/Clip/Hashed가 Render Method로 바뀌었고 기본값이 `Dithered`다.** 디더 투명도는 플립북 알파 가장자리를 지저분하게 만든다 → **`Blended`를 명시해라**
- **Blender 5.0에서 EEVEE 엔진 ID가 `BLENDER_EEVEE_NEXT` → `BLENDER_EEVEE`로 바뀌었다.** 옛 ID를 하드코딩한 렌더 스크립트는 깨진다
- **알파 프리멀티플라이**가 어두운 테두리(halo)의 1순위 원인이다. 프레임 간 **패딩**을 두어 밉맵/bilinear 번짐을 막아라
- 회색조 시트 + `LightEmission = 1`(가산)이면 알파 채널 없이도 빛나는 이펙트가 된다
- 파일 제약: **.png/.tga, 15 MB 이하, 권장 512×512**

**시뮬레이션은 신중하게.** 무거운 유체 시뮬은 과거에 사람이 중단시킨 이력이 있다 — `08_ART_RULES.md` §5. 시뮬 없이 굽는 경로(CC0 VDB 임포트 → 렌더, 절차 생성 PIL)를 먼저 검토해라.
