# 08 ART RULES — 아트디렉션 구속 조건

**참고자료가 아니라 구속 조건이다.** 아래는 전부 실제로 반려당하거나 성능 사고를 낸 뒤에 쓰였다.

---

## 0. 반려 이력 — 같은 실수를 반복하지 마라

2026-09-07, 산곡 FX 1차 프리뷰가 반려됐다. 원문:

> **"지형 이펙트좀 배우고 오삼"**

무엇이 문제였나: **안개를 반투명 구체 `Part`로 만들었다.** 결과가 안개가 아니라 **비눗방울/거품**으로 보였다.

**왜:** 반투명 구체는 어떤 밀도에서도 거품으로 읽힌다. 구체는 명확한 실루엣과 일정한 두께 프로파일을 갖는데 공기는 그런 게 없다. 게임 안개는 **노이즈 텍스처를 카메라 정면 스프라이트로 여러 층 겹쳐서** 만들고, 생애 페이드·카메라 근접 페이드·전역 바람·역광이 붙어야 "공기"로 읽힌다.

그 반성으로 아래 8규칙이 나왔다.

---

## 1. 지형/환경 이펙트 8규칙

1. **안개는 구체가 아니라 스프라이트다.** 노이즈 텍스처를 카메라 정면 빌보드로 **3층** 겹친다 (수면 위 +0.5 / +1.0 / +1.6 m, 폭 3.5~7 m). 장당 불투명도 **0.09~0.16**, 랜덤 회전, 가로로 눌린 비율 **0.55**. **겹침이 밀도를 만든다.**
2. **팝 금지.** 생애 앞뒤로 알파 페이드 — `sin(k·π)^0.6` (빨리 올라와 오래 머물고 천천히 사라짐).
3. **벽 금지.** 카메라 근접 페이드: **4 m 안 0 → 10 m 밖 1.** 큰 스프라이트가 카메라를 스치면 화면이 회색 벽이 된다.
4. **눈높이 아래에만.** 안개는 도랑(계류)과 절벽 발치에 산다. 플레이어 시야를 가리는 곳에 두지 않는다(전투 가독성). 깊이 페이드가 없으므로 **스프라이트 하단을 지면에 약간 묻어 교차선을 숨긴다.**
5. **바람은 하나.** 전역 `WIND` 벡터 **하나**가 안개·솔잎·빛무리·이미터 가속을 전부 움직인다 (Ghost of Tsushima 전역 바람 규칙). 각자 따로 흔들리면 "공기"가 아니라 "파츠"로 보인다.
6. **안개는 역광에서만 읽힌다.** 태양 고도 **10° 저각**, 진행 방향 쪽에 두어 안개 층이 밝게 뜨게. **안개색은 하늘보다 살짝 어둡게**(빛나는 안개 금지). 거리 안개(`Atmosphere`)와 지역 안개(스프라이트)는 함께 튠.
7. **엔진 매핑.** `ParticleEmitter`: `Orientation = FacingCameraWorldUp`, `Shape = Box` / `ShapeStyle = Volume`, `Squash` −0.45(가로로 넓게), `LightEmission` 0.1, `Lifetime` 8~12, `Speed` 5~12 cm/s, `Rate` ≈1.2/앵커.
8. **밀도는 낮게.** 앰비언트는 보이는 순간 과하다. 포말·빛무리·솔잎은 **"있는지 모를" 밀도로.**

**규칙 3 갱신:** `BillboardGui`에 `MaxDistance`/`DistanceLowerLimit`/`DistanceUpperLimit`가 있다 (`01_ENGINE_TRUTH` §5). 근접 페이드를 매 프레임 계산하지 말고 이걸 먼저 시도해라.
**규칙 7 주의:** `Speed 5~12 cm/s`는 **이미 엔진 단위(cm)**다. 28을 또 곱하지 마라.
**규칙 6 실행:** 역광 판정은 `SGK_Spawn_Red` / `SGK_Spawn_Blue` 두 스폰에서 각각 봐야 한다 — 한쪽에서만 예쁘면 안 된다.

---

## 2. 성능 규칙

**fps 레버는 Workspace 인스턴스 수 하나다.** 다른 손잡이가 없다.

| 예산 | 값 |
|---|---|
| 환경 FX — Tier 0 (`Part`) | **≤120** |
| 환경 FX — Tier 1 (`ParticleEmitter`) | **≤30** |
| 전투 FX | **≤90 Part** |

`ViewmodelController_2.lua:302`의 실제 캡:

```lua
local FX_LIMIT = {
    -- ★ 2026-08-19 : 18000(180m) 이었는데 맵이 바뀌면서 남의 이펙트가 전부 사라졌다.
    RANGE = 40000,     -- 400 m
    KUNAI = 8, DRAGONS = 4, DEBRIS = 9, LOGS = 10, PUFFS = 160, XBLADES = 6,
}
```

*"궁극기 한 번에 용 44 + 균열 32 + 바위 38 = 114개, 순간이동 한 번에 연막 28개"*

**규칙:**
- **캡을 넘으면 조용히 버려라** (`if #smokePuffs >= FX_LIMIT.PUFFS then return end`) 또는 오래된 것부터 제거
- **사전 할당 후 재배치.** *"매 프레임 새로 만들면 파츠가 수백 개씩 쌓인다"*
- **`Transparency`만으론 안 사라진다 — `Size`도 줄여라.** 명멸도 알파가 아니라 Size로
- **컬링 반경을 맵보다 작게 잡지 마라** (위 주석의 사고)
- **`PerfProbe`가 3초마다 fps를 찍는다.** FX 전후로 이 숫자를 비교해라 — 유일한 객관 지표다. 현재 안정 상태 **50대 fps** (`06_TERRAIN.md` §4)

---

## 3. 톤 — 이 게임이 무엇인가

**ONLY ONE SHOT** — 1히트 킬 동양 냉병기 대전. 나라별 캐릭터(일본/와키자시, 한국/국궁, 멕시코/마쿠아위틀). **절제·긴장.**
**산곡** — 조선 산간 계류. 무드 = **새벽 물안개**. 소나무 341그루, 억새, 고사리, 바위, 정자, 돌다리.

**146개 프리셋의 장르 태그를 봐라** — `Adventure` / `Cartoon` / `Fantasy` / `Sci-Fi` / `Casual` / `Horror` / `IP`. **대부분 다른 게임 톤이다.**

프리셋을 쓸 때 반드시 확인할 것:
- 채도가 우리보다 높지 않은가 (만화적 원색)
- 밀도가 우리보다 높지 않은가 (규칙 8: "있는지 모를")
- 실루엣이 양식화돼 있지 않은가 (하트, 별, 만화 연기 뭉치)

맞지 않으면 `VFXPreset`의 `Color`/`Size`/`Transparency` 셋으로 눌러보고, 그래도 안 되면 `VFXRecipe`로 내려가 Color/Alpha 키포인트와 `SpawnCount`를 우리 쪽으로 끌어와라.
**톤이 안 맞는 프리셋을 "공짜니까" 쓰는 게 이 인계에서 제일 경계할 실패다.**

---

## 4. 판정은 어떻게 하나

- **판정 근거는 `overdare_screenshot`(3D)과 `overdare_ui_browse`(UI)뿐이다.**
- **오프라인 블렌더 렌더로 인게임 품질을 판단하지 마라.** 과거에 그렇게 판단한 결과물이 §0의 반려를 맞았다.
- 환경 FX는 **60 m 거리 가독성**과 **양 팀 스폰 방향 역광**을 둘 다 확인해야 한다.
- 전투 FX는 **표면별로** 확인한다 — 물 / 여울 / 바위 / 석재 / 흙 / 솔잎 / 풀.
- **UI는 스크린샷에 안 나온다.** `overdare_ui_browse`는 플레이테스트 중에만 동작한다.

---

## 5. 무거운 작업 전에는 비용을 먼저 말해라

사용자 PC는 **노트북**(Ultra 9 275HX, RTX 5080 Laptop, 31 GB RAM)이다.

2026-09-07에 진행 중이던 작업이 중단됐다:
> **"멈춰라, 컴이 노트북이어서 못 버틸 것 같다"**, **"용량 별로 없으니 영상 렌더 하지 말자"**
(대상: 100 m 계류 Mantaflow FLIP 시뮬 1.7 GB 캐시 + 360프레임 렌더)

**규칙: 유체 시뮬·수백 프레임 렌더·GB 단위 캐시는 제안 단계에서 시간·디스크·발열 비용을 먼저 말하고 승인받은 뒤 시작한다.** 프리뷰는 스틸 몇 장으로 끝낸다. 물은 시뮬 대신 셰이더/플립북 접근으로.

블렌더 문서 기준 Mantaflow res 256 × 250프레임 ≈ 최대 ~80 GB. **시뮬 없이 아틀라스를 얻는 경로(CC0 VDB 임포트, 절차 생성)를 먼저 검토해라.**

---

## 6. 코드 규약

- **이름은 전역 유일 접두사로.** 기존 관례: `FX_Puff`, `Ruin_Aim`, `Gukgung_Arrow`, `SGKFX_`
- **모든 프로퍼티 쓰기는 `pcall`**
- **새 스크립트는 `--!strict`로 시작하고 `overdare_validate_lua`를 통과시켜라**
- **이미 있는 것을 다시 만들지 마라** — `_G.HUD.flash`, `UIMotion`, `_G.Sfx`. `02_DO_NOT.md` §8
- **서버가 FX 파츠를 복제하지 않는다.** 서버는 "누가 / 어떤 이펙트"만 알리고 **각 클라이언트가 자기 화면에 그린다** (`BarrierFX.lua` 패턴)
